#include "lists.h"

/**
 * is_palindrome - fn to check if palindromes
 * @head: points to the head
 * Description: is it palindromic
 * Return: 0 or 1
 */
int is_palindrom(listint_t, **head)
{
	listint_t *tail;
	listint_t *currentNode = *head;

	if (*head == NULL)
		return (1);
	/*
	while (currentNode != NULL)
	{

	}
	*/
}
